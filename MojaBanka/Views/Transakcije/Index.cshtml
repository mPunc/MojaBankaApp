@model IEnumerable<MojaBanka.Models.Transakcija>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Iznos_transakcije)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Id_racun)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Iznos_transakcije)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Id_racun)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_transakcija }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_transakcija }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_transakcija })
        </td>
    </tr>
}

</table>
